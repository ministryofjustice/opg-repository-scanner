source:
  directory: './'
  follow_symlinks: false
  exclude:
    - node_modules**/*
    - __samples__**/*
manifests:
  - name: composer
    uses: ComposerParser
  - name: package
    uses: PackageParser
output:
  - location: 'scan-result'
    as: 'json'
