filesystem:
  directory: './'
  follow_symlinks: false

manifests_and_locks:
  - name: composer
    manifest:
      file: '**/composer.json'
      parser: JsonKeyValuePair
      selectors:
        - '.require'
        - '.require-dev'
    lock:
      file: '**/composer.lock'
      parser: JsonKeyValuePair
      selectors:
        - '.packages'
        - '.packages-dev'
