filesystem:
  directory: './'
  follow_symlinks: true

manifests_and_locks:
  - name: composer
    manifest:
      file: '**/composer.json'
      parser: json-key-value
      selectors:
        - '.require'
        - '.require-dev'
    lock:
      file: '**/composer.lock'
      parser: json-key-value
      selectors:
        - '.packages'
        - '.packages-dev'
  - name: package
    manifest:
      file: '**/package.json'
      parser: json-key-value
      selectors:
        - '.dependencies'
        - '.devDependencies'
    lock:
      file: '**/package.lock'
      parser: json-key-value
      selectors:
        - '.dependencies'
