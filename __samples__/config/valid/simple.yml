filesystem:
  directory: './'
  follow_symlinks: true

manifests_and_locks:
  - name: composer
    manifest:
      file: '**/composer.json'
      parser: JsonKeyValuePair
      selectors:
        - '.require'
        - '.require-dev'
    lock:
      file: '**/composer.lock'
      parser: JsonKeyValuePair
      selectors:
        - '.packages'
        - '.packages-dev'
  - name: package
    manifest:
      file: '**/package.json'
      parser: JsonKeyValuePair
      selectors:
        - '.dependencies'
        - '.devDependencies'
    lock:
      file: '**/package.lock'
      parser: JsonKeyValuePair
      selectors:
        - '.dependencies'
