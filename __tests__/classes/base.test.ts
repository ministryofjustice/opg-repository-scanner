import * as process from 'process'
import * as cp from 'child_process'
import * as path from 'path'


// test('test object overwriting on base with new property', async () => {
//     let obj: object = {example: 'test'}
//     const b: base = new base(obj)
//     expect( b.hasOwnProperty('example') ).toBeTruthy()
// })

test('test object overwriting', async () => {


    // Object.assign(t.sub, t.sub);
    // //expect(e.foo).toBe('bar')

    // console.log('names', Object.getOwnPropertyNames(e))
    // console.log('values', Object.values(e))
    // console.log('keys', Object.keys(e))
})
